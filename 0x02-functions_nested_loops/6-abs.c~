#include "holberton.h"
/**
 * _abs - outputs absolute value of a number
 * @int: integer for validation
 * Return: int
 */
int _abs(int n)
{
if (n < 0)
{
n = n * (-1);
return (n);
}
else
{
return (n);
}
}
