#include "holberton.h"

/**
 * _memcpy - copies a block of memory
 *
 * @dest: pointer
 * @src: block to copy
 * @n: number of spaces
 * Return: dest
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	int i;

	for (i = 0; i < 0; i++)
	{
	        dest[i] = src[i];
	}
	return (dest);
}
